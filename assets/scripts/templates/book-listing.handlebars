{{#each books as |book|}}

  {{> partial}}
<!--   Don't uncomment yet -->

 <!-- Developers, why are we using data-id and not just id? -->
<ul list-id="{{book.id}}">
  <li>
  <h1 data-id="{{book.id}}"> <small>Title: {{book.title}}</small> <small>Author: {{book.author}}</small></h1>
  </li>
</ul>

{{/each}}
